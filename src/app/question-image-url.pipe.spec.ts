import { QuestionImageUrlPipe } from './question-image-url.pipe';

describe('QuestionImageUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionImageUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
